type: object
properties:
  parentId:
    type: string
    format: uuid
    description: >
      New parent directory for the restored item.
      Required when the item's original parent is trashed,
      or when the item is a descendant of a trashed item.
