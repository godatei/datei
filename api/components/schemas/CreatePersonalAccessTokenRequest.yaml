type: object
properties:
  label:
    type: string
    minLength: 1
    maxLength: 255
    description: Human-readable label describing the token's purpose; omit for unlabeled tokens
  expiresAt:
    type: string
    format: date-time
    description: Expiration timestamp; omit for "never expires"
