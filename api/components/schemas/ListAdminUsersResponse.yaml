type: object
required:
  - items
  - total
properties:
  items:
    type: array
    items:
      $ref: './AdminUserListItem.yaml'
    description: Array of users
  total:
    type: integer
    description: Total number of users (before pagination)
