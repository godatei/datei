type: object
required:
  - items
  - total
properties:
  items:
    type: array
    items:
      $ref: './TrashedDatei.yaml'
    description: Array of trashed Datei objects
  total:
    type: integer
    description: Total number of items (before pagination)
