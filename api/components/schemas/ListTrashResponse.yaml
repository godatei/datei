type: object
required:
  - items
  - total
properties:
  items:
    type: array
    items:
      $ref: './TrashedFile.yaml'
    description: Array of trashed File objects
  total:
    type: integer
    description: Total number of items (before pagination)
