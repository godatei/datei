type: object
required:
  - success
properties:
  success:
    type: boolean
    description: Whether the IMAP connection and login succeeded
  message:
    type: string
    description: Error detail when the connection failed
