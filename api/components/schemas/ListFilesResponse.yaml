type: object
required:
  - items
  - total
properties:
  items:
    type: array
    items:
      $ref: './File.yaml'
    description: Array of File objects
  total:
    type: integer
    description: Total number of items (before pagination)
