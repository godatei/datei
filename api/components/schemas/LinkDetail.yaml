allOf:
  - $ref: './Link.yaml'
  - type: object
    required:
      - files
    properties:
      files:
        type: array
        items:
          $ref: './File.yaml'
        description: Top-level files shared by the link
