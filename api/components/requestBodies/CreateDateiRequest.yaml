type: object
properties:
  parentId:
    type: string
    format: uuid
    description: Parent directory ID (omit for root)
  name:
    type: string
    description: Name for the new Datei
  file:
    type: string
    format: binary
    description: File data (optional, omit for directories)
