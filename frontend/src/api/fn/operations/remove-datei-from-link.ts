/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface RemoveDateiFromLink$Params {
  id: string;
  dateiId: string;
}

export function removeDateiFromLink(http: HttpClient, rootUrl: string, params: RemoveDateiFromLink$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, removeDateiFromLink.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('dateiId', params.dateiId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

removeDateiFromLink.PATH = '/api/v1/links/{id}/dateien/{dateiId}';
